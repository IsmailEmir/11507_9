Вариант 2
(7 баллов) Напишите PropertyLogger. Метод GetLog(object obj) должен через 
рефлексию вернуть список строк (НЕ ВЫВЕСТИ В КОНСОЛЬ) "[ИмяСвойства]: [Значение]" 
для всех свойств, у которых значение не null.

(7 баллов) Напишите ConcurrentSystem: один поток-продюсер генерирует
 объекты User и записывает их в очередь, а второй поток-консьюмер 
 одновременно с ним считывает их из очереди и записывает в List.

(1 балл) Модифицируйте код: консьюмер должен не просто записывать 
объект, а каждый раз вызывать PropertyLogger.GetLog() для извлечения
 состояния объекта перед записью в список (сохранять значения не надо).