namespace IO;

public class Diary
{
    //Дневник (базовая версия)
    // Console.WriteLine("Введите содержимое файла:");
    // var input = Console.ReadLine();
    // var date = DateTime.Now;
    // var path = Path.Combine(Directory.GetCurrentDirectory(), "Diary.txt");
    //
    // input = input + " Дата изменения: " + date.ToString("dd/MM/yyyy HH:mm");
    // File.AppendAllText(path, input + "\n");
}