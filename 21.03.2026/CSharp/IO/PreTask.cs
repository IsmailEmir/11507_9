namespace IO;

public class PreTask
{
    //Создание папки по пути и копирование файлов
    // Console.WriteLine($"Current directory: {Directory.GetCurrentDirectory()}");
    //
    // var currentDirectory = Directory.GetCurrentDirectory();
    // var archivePath = Path.Combine(currentDirectory, "Archive");
    // if (!Directory.Exists(archivePath))
    //     Directory.CreateDirectory(archivePath);
    //
    // var homeDir = Environment.GetFolderPath(Environment.SpecialFolder.UserProfile);
    // var userFolder = Path.Combine(homeDir, "Downloads");
    //
    // var files = Directory.Exists(userFolder)
    //     ? Directory.GetFiles(userFolder)
    //     : [];
    //
    // foreach (var file in files)
    // {
    //     var extension = Path.GetExtension(file);
    //     if (extension == ".txt")
    //     {
    //         var destinationPath = Path.Combine(archivePath, Path.GetFileName(file));
    //         File.Copy(file, destinationPath);
    //     }
    // }
}